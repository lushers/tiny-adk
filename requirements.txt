# 简化版 ADK 依赖

# 核心依赖
pydantic>=2.0.0  # 数据验证

# LLM 集成
openai>=1.0.0  # 用于连接 OpenAI 兼容的 API（包括 vLLM）

# 配置文件支持
pyyaml>=6.0  # 用于加载 YAML 配置文件

# Web 服务
fastapi>=0.100.0  # Web 框架
uvicorn>=0.23.0   # ASGI 服务器

# 其他可选依赖
# requests  # 如果需要实现真实的网页搜索工具
# python-dotenv  # 用于从 .env 文件自动加载环境变量
